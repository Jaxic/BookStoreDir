---
// Emergency test - absolute minimal page
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Emergency Test</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
            background: #f0f0f0;
            margin: 0;
        }
        .box {
            background: white;
            padding: 20px;
            margin: 10px 0;
            border: 3px solid #007acc;
            border-radius: 10px;
            font-size: 18px;
        }
        .success {
            background: #d4edda;
            border-color: #28a745;
            color: #155724;
        }
    </style>
</head>
<body>
    <div class="box success">
        <h1>🚨 EMERGENCY TEST</h1>
        <p><strong>✅ If you can see this, basic HTML works!</strong></p>
        <p>Time: <span id="time">Loading...</span></p>
        <p>Screen: <span id="screen">Loading...</span></p>
    </div>
    
    <div class="box">
        <h2>📱 Device Info</h2>
        <p>User Agent: <span id="ua">Loading...</span></p>
        <p>JavaScript: <span id="js-status">❌ DISABLED</span></p>
    </div>
    
    <div class="box">
        <h2>🗺️ Map Test</h2>
        <p>This should show a simple map placeholder:</p>
        <div style="width: 100%; height: 200px; background: #e9ecef; border: 2px dashed #6c757d; display: flex; align-items: center; justify-content: center; font-size: 16px; color: #6c757d;">
            📍 STATIC MAP PLACEHOLDER<br>
            (This proves CSS layout works)
        </div>
    </div>

    <script>
        // This will only run if JavaScript works
        document.getElementById('js-status').innerHTML = '✅ ENABLED';
        document.getElementById('time').innerHTML = new Date().toLocaleString();
        document.getElementById('screen').innerHTML = window.innerWidth + 'x' + window.innerHeight;
        document.getElementById('ua').innerHTML = navigator.userAgent.substring(0, 50) + '...';
    </script>
</body>
</html> 