---
import { parseBookstores } from '../lib/csv/parser';
import { processBookstore } from '../lib/processors/bookstore';
import type { ProcessedBookstore } from '../types/bookstore';
import StoreMapFixed from '../components/map/StoreMapFixed';
import path from 'path';

// Load and process bookstore data
const csvPath = path.join(process.cwd(), 'src', 'data', 'bookstores.csv');
const { records } = await parseBookstores(csvPath);
const stores: ProcessedBookstore[] = records.map(processBookstore);
console.log(`Loaded ${stores.length} stores for full-screen map`);
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Full-Screen Bookstore Map - BookDir</title>
    <meta name="description" content="Interactive full-screen map showing {stores.length} independent bookstores across Canada. Find stores near you with detailed information and directions.">
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50">
    <!-- Navigation Header -->
    <header class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <!-- Left side - Back button and title -->
          <div class="flex items-center">
            <a href="/" class="flex items-center text-gray-600 hover:text-gray-900 mr-4">
              <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
              </svg>
              <span class="text-sm font-medium">Back to Home</span>
            </a>
            <h1 class="text-xl font-semibold text-gray-900">📚 Full-Screen Map</h1>
          </div>
          
          <!-- Right side - Navigation links -->
          <nav class="hidden md:flex space-x-6">
            <a href="/" class="text-gray-600 hover:text-gray-900 text-sm font-medium">Home</a>
            <a href="/stores" class="text-gray-600 hover:text-gray-900 text-sm font-medium">Browse Stores</a>
            <a href="/map" class="text-blue-600 text-sm font-medium">Full-Screen Map</a>
          </nav>
          
          <!-- Mobile menu button -->
          <button class="md:hidden p-2 rounded-md text-gray-600 hover:text-gray-900 hover:bg-gray-100">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>
      </div>
    </header>

    <!-- Main Content -->
    <main class="flex-1">
      <!-- Map Info Bar -->
      <div class="bg-blue-50 border-b border-blue-200 px-4 py-3">
        <div class="max-w-7xl mx-auto">
          <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <div class="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center">
                  <span class="text-blue-600 text-sm font-medium">{stores.length}</span>
                </div>
              </div>
              <div class="ml-3">
                <p class="text-sm font-medium text-blue-900">
                  Showing {stores.length} bookstores across Canada
                </p>
                <p class="text-xs text-blue-700">
                  Click markers for store details • Use controls to navigate • Enable location for nearby stores
                </p>
              </div>
            </div>
            <div class="mt-3 sm:mt-0 flex gap-2">
              <a 
                href="/" 
                class="inline-flex items-center px-3 py-2 border border-blue-300 shadow-sm text-sm leading-4 font-medium rounded-md text-blue-700 bg-white hover:bg-blue-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500"
              >
                <svg class="w-4 h-4 mr-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
                </svg>
                Home
              </a>
              <a 
                href="/stores" 
                class="inline-flex items-center px-3 py-2 border border-blue-300 shadow-sm text-sm leading-4 font-medium rounded-md text-blue-700 bg-white hover:bg-blue-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500"
              >
                <svg class="w-4 h-4 mr-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 10h16M4 14h16M4 18h16"></path>
                </svg>
                Browse All
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- Map Container -->
      <div class="h-[calc(100vh-140px)] w-full">
        <StoreMapFixed client:only="react" stores={stores} height="100%" />
      </div>
    </main>
  </body>
</html>

<style>
  /* Ensure proper layout */
  html, body {
    height: 100%;
    margin: 0;
    padding: 0;
  }
  
  body {
    display: flex;
    flex-direction: column;
  }
  
  /* Custom map styles */
  :global(.leaflet-popup-content-wrapper) {
    border-radius: 8px;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  }
  
  :global(.leaflet-popup-tip) {
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  }
  
  :global(.bookstore-marker:hover) {
    transform: scale(1.1);
  }
  
  :global(.leaflet-control-zoom) {
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  }
  
  :global(.leaflet-control-zoom a) {
    color: #374151;
    font-weight: 600;
  }
  
  :global(.leaflet-control-zoom a:hover) {
    background-color: #f3f4f6;
  }
</style> 