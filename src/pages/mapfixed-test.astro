---
import { parseBookstores } from '../lib/csv/parser';
import { processBookstore } from '../lib/processors/bookstore';
import type { ProcessedBookstore } from '../types/bookstore';
import StoreMapFixed from '../components/map/StoreMapFixed';
import path from 'path';

// Load and process bookstore data
const csvPath = path.join(process.cwd(), 'src', 'data', 'bookstores.csv');
const { records } = await parseBookstores(csvPath);
const stores: ProcessedBookstore[] = records.map(processBookstore);
console.log(`Loaded ${stores.length} stores for Leaflet npm test`);
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Leaflet NPM Test - BookDir</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: system-ui, -apple-system, sans-serif;
      }
    </style>
  </head>
  <body>
    <div class="min-h-screen bg-gray-50">
      <!-- Header -->
      <header class="bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-4 py-4">
          <div class="flex items-center justify-between">
            <div>
              <h1 class="text-2xl font-bold text-gray-900">Leaflet NPM Test</h1>
              <p class="text-sm text-gray-600">Testing npm-based Leaflet implementation (no CDN)</p>
            </div>
            <div class="text-right">
              <div class="text-sm text-gray-500">
                <span class="font-medium">{stores.length}</span> bookstores loaded
              </div>
              <div class="text-xs text-gray-400">
                Using Leaflet from npm package
              </div>
            </div>
          </div>
        </div>
      </header>

      <!-- Map Container -->
      <main class="max-w-7xl mx-auto px-4 py-6">
        <div class="bg-white rounded-lg shadow-lg overflow-hidden">
          <div class="p-4 border-b">
            <h2 class="text-lg font-semibold text-gray-900">Interactive Store Map</h2>
            <p class="text-sm text-gray-600">
              ✅ This uses npm Leaflet imports instead of CDN scripts for reliable loading.
            </p>
          </div>
          
          <!-- Map -->
          <div style="height: 600px;">
            <StoreMapFixed 
              client:only="react" 
              stores={stores}
              height="100%"
            />
          </div>
        </div>

        <!-- Comparison Panel -->
        <div class="mt-6 grid grid-cols-1 md:grid-cols-2 gap-4">
          <div class="bg-white p-4 rounded-lg shadow">
            <h3 class="font-semibold text-gray-900 mb-2">✅ NPM Leaflet Advantages</h3>
            <ul class="text-sm text-gray-600 space-y-1">
              <li>• Direct npm imports (import L from 'leaflet')</li>
              <li>• No CDN loading delays or failures</li>
              <li>• Perfect SSR/Astro compatibility</li>
              <li>• TypeScript support included</li>
              <li>• Bundled with your app</li>
              <li>• Works offline</li>
            </ul>
          </div>
          
          <div class="bg-white p-4 rounded-lg shadow">
            <h3 class="font-semibold text-gray-900 mb-2">❌ CDN Issues We Fixed</h3>
            <ul class="text-sm text-gray-600 space-y-1">
              <li>• "Loading Leaflet libraries" infinite loop</li>
              <li>• Race conditions between script loading</li>
              <li>• SSR hydration mismatches</li>
              <li>• Network dependency for core functionality</li>
              <li>• Version inconsistencies</li>
              <li>• Security concerns with external scripts</li>
            </ul>
          </div>
        </div>

        <!-- Status Panel -->
        <div class="mt-4 bg-blue-50 border border-blue-200 rounded-lg p-4">
          <h3 class="font-semibold text-blue-900 mb-2">🎯 Implementation Status</h3>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-sm">
            <div>
              <h4 class="font-medium text-blue-800">Core Features ✅</h4>
              <ul class="text-blue-700 mt-1">
                <li>• Interactive markers</li>
                <li>• User location detection</li>
                <li>• Distance calculations</li>
                <li>• Popup information</li>
                <li>• Map controls</li>
              </ul>
            </div>
            <div>
              <h4 class="font-medium text-blue-800">Performance ✅</h4>
              <ul class="text-blue-700 mt-1">
                <li>• Fast initialization</li>
                <li>• No loading screens</li>
                <li>• Responsive design</li>
                <li>• Efficient rendering</li>
                <li>• Memory management</li>
              </ul>
            </div>
            <div>
              <h4 class="font-medium text-blue-800">Next Steps 🚀</h4>
              <ul class="text-blue-700 mt-1">
                <li>• Add marker clustering</li>
                <li>• Search and filtering</li>
                <li>• Custom map themes</li>
                <li>• Routing integration</li>
                <li>• Mobile optimizations</li>
              </ul>
            </div>
          </div>
        </div>
      </main>
    </div>
  </body>
</html> 